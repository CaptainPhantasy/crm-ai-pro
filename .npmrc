# NPM Configuration for CRM-AI-PRO
# This configuration ensures compatibility with Next.js and modern packages

# Force legacy peer dependency resolution to prevent conflicts
legacy-peer-deps=true

# Optional: Set registry if using a private registry
# registry=https://registry.npmjs.org/

# Optional: Increase timeout for slow installs
# fetch-timeout=600000

# Optional: Save exact versions
# save-exact=true