'use client'

/**
 * VoiceNavigationBridgeSimple
 *
 * This component is DISABLED - use VoiceNavigationBridge instead
 * 
 * @deprecated This component is no longer used. VoiceNavigationBridge provides the same functionality
 * with additional features and better integration with the voice conversation provider.
 */
export function VoiceNavigationBridge() {
  // This component is disabled - use VoiceNavigationBridge instead
  console.warn('[VoiceNavigationBridgeSimple] This component is deprecated')
  return null
}
